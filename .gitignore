# Python virtual environments
.venv/
venv/
env/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# R artifacts
.Rhistory
.RData
.Rproj.user/
*.Rproj

# Data files (never commit)
*.csv
*.rds
*.RData
*.sqlite
*.db
*.cas
*.geo
*.pop
*.shp
*.shx
*.dbf
*.prj

# Office temp files
~$*

# OS files
.DS_Store
Thumbs.db
desktop.ini

# OneDrive sync conflict files (named like foo-239GX64.py, foo-DL29GY3.toml)
*-[A-Z0-9][A-Z0-9][A-Z0-9][A-Z0-9][A-Z0-9][A-Z0-9][A-Z0-9].py
*-[A-Z0-9][A-Z0-9][A-Z0-9][A-Z0-9][A-Z0-9][A-Z0-9][A-Z0-9].toml
*-[A-Z0-9][A-Z0-9][A-Z0-9][A-Z0-9][A-Z0-9][A-Z0-9][A-Z0-9].R
*-[A-Z0-9][A-Z0-9][A-Z0-9][A-Z0-9][A-Z0-9][A-Z0-9][A-Z0-9]-*.py
*-[A-Z0-9][A-Z0-9][A-Z0-9][A-Z0-9][A-Z0-9][A-Z0-9][A-Z0-9].json

# Secrets
.env
*.env
secrets.*

# Build artifacts
*.egg-info/
build/
dist/

# ── Personal content (never push to GitHub) ──────────────────────────────────
# These contain the user's personal research data.
# They stay on your machine but are never committed.
metis/journal/
metis/inbox/
metis/inputs/meetings/
metis/outputs/
metis/projects/active/
metis/archive/
metis/basket/
metis/knowledge/library/papers/
metis/knowledge/library/open-access-books/
metis/knowledge/library/disease-areas/
metis/knowledge/library/reading-lists/
metis/knowledge/library/people-organizations/
metis/system/app/data/
metis-eval-prompt.md
metis-functional-test.docx

# Personal agent overlay files. Only *-context.md and the user-specific
# `sources.md` overlays are gitignored — all generic skill.md / system-prompt.md
# files are tracked so a fresh clone is functional out of the box.
metis/agents/**/*-context.md
metis/agents/librarian/sources.md
metis/agents/news-radar/sources.md

# Private agent: Release Coordinator (contains personal repo graph, credentials
# patterns, and PH-specific propagation rules — never ship to GitHub)
metis/agents/release-coordinator/

# Per-user config and session state
metis/system/config/user-preferences.json
metis/system/config/user-config.yaml
metis/system/config/session-log.md
metis/system/config/SETUP-*.md
metis/system/config/claude-project-instructions.md
metis/system/config/implementation-progress.json
metis/system/config/thinking-profile.yaml
# Persona file contains real user's name — each user generates their own via /metis_config
metis/system/config/metis-persona.md
# Eval and audit reports may contain personal data references
metis/system/config/eval-report.md
# Working plan docs reference personal projects / milestones
metis/system/config/implementation-plan.md
metis/system/config/feature-backlog.md
# Design export HTML previews contain hardcoded demo user data
metis/system/design-docs/claude-design-export/

# PH-specific install scripts (Metis_PH only — not part of base Metis)
metis/system/install/seed_ph_database.py

# Per-machine runner scripts (generated by setup-mcp.sh with local paths)
metis/system/app-py/run.sh
system/app-py/run.sh

# Local-only tools and state — never push to GitHub
system/config/local/

# Personal config files (created after personalisation)
metis/system/config/SETUP*.md
metis/system/config/add-new-project.md
metis/system/config/claude-project-instructions.md
metis/system/config/implementation-progress.json
metis/system/config/workflows/
metis/system/design-docs/CLAUDE_DESIGN_BRIEF_TOMORROW.md

# Personal literature inputs
metis/inputs/literature/

# Indexer runtime logs (contain local absolute paths)
metis/system/install/indexer.log
metis/system/install/*.log

# PH-specific seeding scripts
metis/system/install/seed_ph_database.py

# Demo/mockup seeding + filming tools (personal only — never publish)
metis/system/install/seed_mockup_demo.py
system/install/seed_mockup_demo.py
system/install/setup_demo_root.py
system/install/demo_setup.sh
.claude/skills/demo-builder/

# Test suite (personal only — not published to public repos)
metis/system/tests/

# Session/build artifacts — never publish
outputs/
# Ad-hoc export folder for generated decks/exports. `outputs/` above does not
# cover it — the name differs — and the .pptx it holds run to tens of MB.
/Claude outputs/
metis/system/app-py/DESIGN_BRIEF.md
metis/system/config/install-state.json
metis/system/design-docs/

# Old R app runtime files (kept locally, not published)
system/app/History/
metis/system/app/History/
metis/system/app/R/data_store.R
metis/system/app/R/mod_meetings.R
metis/system/app/R/mod_news.R

# Legacy personal items
resume.docx
Prompt.docx

# ── Post-restructure rules (repo root promoted from metis/ in commit 98d5e93) ─
# The metis/ prefixed rules above no longer match after the restructure.
# These bare rules enforce the same exclusions at the new root.

# Personal research content
journal/
inbox/
inputs/literature/
inputs/meetings/
inputs/demo/
outputs/
projects/active/
archive/
basket/

# Library content — PDFs and local paths
knowledge/library/papers/
knowledge/library/open-access-books/
knowledge/library/disease-areas/
knowledge/library/reading-lists/
knowledge/library/people-organizations/
knowledge/library/ph-background/
knowledge/library/DOWNLOAD-RESULTS*.txt
knowledge/library/MANUAL-DOWNLOADS-COMPLETE.md
knowledge/library/download-library-*.sh

# Database files
system/app/data/
system/app-py/data/

# Personal agent overlay files
agents/**/*-context.md
agents/librarian/sources.md
agents/news-radar/sources.md

# Private agent (personal repo graph, credential patterns)
agents/release-coordinator/

# Per-user config
system/config/user-preferences.json
system/config/user-config.yaml
system/config/session-log.md
system/config/SETUP*.md
system/config/claude-project-instructions.md
system/config/implementation-progress.json
system/config/thinking-profile.yaml
system/config/metis-persona.md
system/config/eval-report.md
system/config/eval-results.json
system/config/implementation-plan.md
system/config/feature-backlog.md
system/config/install-state.json

# Runtime / machine-local artifacts
system/app-py/.metis-port
system/app-py/eval-results.json
system/app-py/DESIGN_BRIEF.md
system/install/seed_ph_database.py
system/install/seed_mockup_demo.py
system/install/*.log
system/tests/
system/design-docs/

# Session memory and control-room state (personal activity logs)
01_control-room/

# Local workflow scripts with machine-specific paths
system/config/workflows/

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# Claude Code agent worktrees — transient per-agent branches, never committed
.claude/worktrees/

# Inbox staging area for code review — transient personal files
inputs/code/

# Apply_proposal creates timestamped backups — never committed
*.bak.20*
# ...and the hyphenated form. Tools in this repo write BOTH shapes — `--suffix
# .bak-20260828T151953` from the config writers, `.bak.20260828` from others —
# and only the dotted one was ignored. Two personal-config backups
# (domain-overrides.local.json, user-preferences.json) were sitting untracked
# and offerable to any `git add -A`; `.local.` files are exactly what must not
# reach a published repo.
*.bak-20*
*.bak-????????T??????
skill.md.bak.*

# Metis_PH domain courses (not part of virgin base)
knowledge/courses/hat-surveillance-methods-for-field-epidemiologists-in-drc/

# Metis_PH domain content (tracked locally, not in virgin base)
knowledge/courses/ntd-elimination/
knowledge/courses/spatial-epi-ntd-upskilling/
knowledge/domains/hat-post-elimination-surveillance/
knowledge/library/hat/
knowledge/library/concepts/dhis2/hat-stage-confirmation-program-stage.md
system/config/.first-run

# MCP server runtime health snapshot (written each server start)
system/config/mcp-health.json
metis/system/config/mcp-health.json

# Stray nested dir (local DB + system copy — never publish)
/metis/

# Local Windows Sandbox configs — contain the user's absolute host path
*.local.wsb

# Private domain/personal overrides — field- and institution-specific sensitive
# patterns (e.g. national case-registry formats, your institute name). Loaded at
# runtime by the PII scanner + anonymiser; NEVER published to any repo/edition.
system/config/domain-overrides.local.json

# PaperQA runtime index cache (rebuilt at runtime by paperqa_search.py)
system/app/data/paperqa_index/

# The /basket SKILL is code (the top-level basket/ data drop-zone stays ignored)
!.claude/skills/basket/
!.claude/skills/basket/**

# Preserved curated reference seed corpus (local only)
system/config/seed-dois.local.md

# Encrypted DB backups — OneDrive-synced, never in git
system/app/data/cloud-backups/
*.sqlite.enc

# Runtime logs — never ship (contain local paths)
system/config/logs/
*.log

# Owner's private DEV-ONLY evaluation suite — never shipped (see tests/README.md)
tests/functional/clickthrough.py
tests/functional/button_intents.py
tests/functional/promise_runner.py
tests/functional/orchestration.py
tests/functional/routing_eval.py
tests/security/
tests/fixtures/
tests/promises.yaml
tests/README.md

# Machine-specific runtime state (not for the repo)
system/config/mcp-health*.json
tests/.audit-logs/
.claude/plan.md
# Graphify — codebase graph output (rebuilt on demand)
graphify-out/
.graphifyignore
# NOTE: graphify-knowledge/ is in .git/info/exclude (not here)
# so Graphify can read it but git won't track the 1,000+ generated files.

# Stray office docs
*.docx

# Base-shell scrub name list (maintainer identity — never ship)
tools/base-shell/scrub-names.txt

# Documents fetched by an installed background pack — the MANIFEST ships, the PDFs
# do not (that is the whole point of a pack: small repo, no redistributing other
# people's work). The folder is recreated on install.
knowledge/library/open-access-books/HAT Elimination Monitoring/

# Local JSON API bearer token — generated per machine, never shared
system/config/api-token.txt

# Self-signed localhost certificate for the Office add-in — per machine, never shared
system/config/certs/

# Promise-harness trend log — runtime metric output, regenerated per run.
system/config/promise-trend.jsonl

# Procedure-correction payloads — carry DB fixes between computers via OneDrive,
# not git (they contain private research paths).
agents/**/_procedure-corrections-*.json

# What Metis has learned about how to behave — personal, like metis-persona.md.
# Append-only ledger of confirmed preferences and corrections, written by
# record_persona_learning(). Stays local; the SHAPE of this file is in
# metis-response-contract.md, which IS tracked.
system/config/metis-learned.md

# Course source PDFs (brochures, syllabi) — keep the specs, not the binaries
knowledge/courses/*/sources/*.pdf

# OneDrive conflict copies of runtime output. OneDrive appends the hostname when
# two machines write the same file, so these appear as eval-results-5XLQDJ4-2.json
# and multiply. The canonical name is already ignored above; the forks are noise.
system/config/eval-results-*.json
system/config/mcp-health-*.json

# The corpus-grounding hook's trigger cache — derived from the profile, rebuilt
# twice a day, machine-local.
system/config/.corpus-triggers.json

# Codex hooks — machine-local, contains absolute Windows/WSL paths by design.
# Same class as .claude/settings.local.json: a launcher config, not source.
.codex/hooks.json

# The redactor names every identifier it removes, so it cannot be published by
# the thing it exists to protect — same reason as agents/release-coordinator/.
# It still reaches the second machine via OneDrive, which syncs untracked files.
tools/strip-identity.py

# ── Personal-overlay tooling: this computer only, never committed ──────────
# The three-layer architecture says the personal overlay is never committed,
# but nothing enforced it for *tools*. These three hard-code an absolute home
# path, a real institutional address, and named internal programmes — and they
# were committed on 2026-09-02 into repositories that are PUBLIC on GitHub
# (both return 200 unauthenticated; checked the same day). They are untracked
# again and stay on this machine, where they work exactly as before.
#
# The generic course tools next to them — check_course_dois.py,
# unwrap_course_prose.py — remain tracked, because CLAUDE.md documents them and
# a documented tool missing from the repo is a broken instruction on the second
# machine. The address in the first is configuration now, not source.
tools/harvest_hat_book_figures.py
tools/harvest_hat_manual_figures.py
tools/update_hat_library.py
